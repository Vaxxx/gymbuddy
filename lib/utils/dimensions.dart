import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Dimensions {
  //colors
  static const backGroundColor = Color(0xFF202020);
  static const kPrimaryColor = Color(0xFFFFBD73);
  static const greyColor = Color(0xFF9E9E9E);
  static const whiteColor = Color(0xFFFFFFFF);
  static const deepGreyColor = Color(0xFF424242);
  static const lightGreyColor = Color(0xFF757575);
  static const blackColor = Color(0xFF000000);
  static const lightBlackColor = Color(0xFF202020);
  static const redColor = Color(0xFFC62828);
  static const lightRedColor = Color(0xFFE57373);
  static const deepGreenColor = Color(0xFF00695C);
  static const lightGreenColor = Color(0xFF80CBC4);
  static const purpleColor = Color(0xFF311B92);
  // static const deepBlueColor = Color(0xFF2C387E);
  static const deepBlueColor = Color(0xFF151c48);
  static const indigoColor = Color(0xFF1A237E);
  static const lightBlue = Color(0xFFE3F2FD);
  static final greenOpacityColor = Colors.green.withOpacity(0.5);
  static const Color success = Color.fromRGBO(45, 206, 137, 1.0);
  static const Color warning = Color.fromRGBO(251, 99, 64, 1.0);
  static const Color info = Color.fromRGBO(44, 168, 255, 1.0);

  //dimensions
  static double SCREEN_HEIGHT = Get.context!.height; //805.3333333333334
  static double SCREEN_WIDTH = Get.context!.width; //384.0

  //heights
  static double height2 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 2);
  static double height3 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 3);
  static double height4 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 4);
  static double height5 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 5);
  static double height8 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 8);
  static double height10 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 10);
  static double height12 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 12);
  static double height13 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 13);
  static double height15 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 15);
  static double height20 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 20);
  static double height25 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 25);
  static double height30 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 30);
  static double height35 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 35);
  static double height40 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 40);
  static double height50 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 50);
  static double height60 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 60);
  static double height80 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 80);
  static double height100 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 100);
  static double height150 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 150);
  static double height200 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 200);
  static double height300 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 300);
  static double height350 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 350);
  static double height450 = SCREEN_HEIGHT / (SCREEN_HEIGHT / 450);

//widths
  static double width2 = SCREEN_WIDTH / (SCREEN_WIDTH / 2);
  static double width3 = SCREEN_WIDTH / (SCREEN_WIDTH / 3);
  static double width4 = SCREEN_WIDTH / (SCREEN_WIDTH / 4);
  static double width5 = SCREEN_WIDTH / (SCREEN_WIDTH / 5);
  static double width10 = SCREEN_WIDTH / (SCREEN_WIDTH / 10);
  static double width15 = SCREEN_WIDTH / (SCREEN_WIDTH / 15);
  static double width20 = SCREEN_WIDTH / (SCREEN_WIDTH / 20);
  static double width25 = SCREEN_WIDTH / (SCREEN_WIDTH / 25);
  static double width30 = SCREEN_WIDTH / (SCREEN_WIDTH / 30);
  static double width40 = SCREEN_WIDTH / (SCREEN_WIDTH / 40);
  static double width50 = SCREEN_WIDTH / (SCREEN_WIDTH / 50);
  static double width60 = SCREEN_WIDTH / (SCREEN_WIDTH / 60);
  static double width100 = SCREEN_WIDTH / (SCREEN_WIDTH / 100);
  static double width150 = SCREEN_WIDTH / (SCREEN_WIDTH / 150);
  static double width200 = SCREEN_WIDTH / (SCREEN_WIDTH / 200);
  static double width250 = SCREEN_WIDTH / (SCREEN_WIDTH / 250);
  static double width300 = SCREEN_WIDTH / (SCREEN_WIDTH / 300);
}
